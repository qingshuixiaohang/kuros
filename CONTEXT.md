# 鸣潮玩家攻略与内容社区

本上下文描述社区中的用户、内容和互动关系，作为需求讨论、Issue 和实现规格的统一业务词汇。

## 参与者

**游客**：未建立登录身份的访问者，可以阅读公开内容，但不能执行需要身份归属的互动行为。
_避免_：匿名用户、未登录用户（在业务讨论中统一使用“游客”）

**用户**：已经建立社区身份的玩家，可以创建内容并参与社区互动。
_避免_：账号（“账号”仅用于描述登录凭据，不作为参与者名称）

**作者**：创建某一篇帖子或评论的用户。
_避免_：发布者（除非特指发布动作）

**管理员**：负责处理违规内容和社区秩序的特殊用户。
_避免_：运营、版主（当前统一使用“管理员”）

## 内容

**帖子**：由用户发布、可被其他用户阅读和互动的社区内容。
_避免_：文章、动态（它们是帖子类型或展示叫法，不作为总称）

**攻略帖**：以角色培养、配队、声骸、探索或战斗方法为主题的帖子类型。
_避免_：攻略文章、指南

**普通帖子**：不属于攻略帖、用于交流和分享的帖子类型。
_避免_：动态、社区文章

**官方资讯**：来源于官方渠道、用于说明版本、活动或公告的信息内容，与用户发布的帖子分开。
_避免_：新闻、公告帖（“新闻”是页面分类，“公告帖”会混淆内容来源）

**评论**：用户针对某一篇帖子发表的短内容，属于该帖子的互动记录。
_避免_：回复（除非特指评论之间的回复关系）

**一级回复**：针对评论发表的评论，最多直接归属于一条评论，不继续向下嵌套。
_避免_：评论树、楼中楼（当前业务只采用一级回复）

**已发布帖子**：对符合可见条件的访问者公开展示、可以被互动的帖子。
_避免_：上线帖子、有效帖子

**已删除帖子**：作者或管理员主动移除后，对普通访问者不可见的帖子。
_避免_：物理删除（这是实现方式，不是业务术语）

**内容分类**：帖子所属的有限主题类别，用于组织攻略内容和筛选浏览。
_避免_：栏目（页面布局概念）、标签

**内容标签**：作者为帖子补充的主题关键词，用于表达更细的内容特征和辅助搜索。
_避免_：话题、分类

## 关系与行为

**个人中心**：展示某个用户的公开资料、内容统计以及其发布内容和评论的页面区域。
_避免_：个人主页（页面可以这样称呼，但业务概念统一使用“个人中心”）

**公开资料**：用户允许其他访问者看到的昵称、头像和个人简介，不包括手机号等登录信息。
_避免_：用户详情（范围不明确）

**评论热度**：用于评论排序的相对受欢迎程度，主要反映评论获得的社区互动。
_避免_：评论时间（时间是另一种排序依据）

**写操作**：会新增、修改或删除社区数据的行为，包括评论、点赞、收藏、关注和发布内容。
_避免_：交互操作（范围过于宽泛，阅读也属于交互）

**登录会话**：用户完成身份验证后，在一段连续访问期间被社区识别为同一用户的状态。会话由 SaToken 管理，Token 和权限数据存储在 Redis 中，支持多实例共享和服务重启不失效。
_避免_：登录信息、令牌（它们是实现或凭据概念）

**角色**：RBAC 模型中用户所具备的身份集合（如 USER、ADMIN），由 `sys_role` 表定义，通过 `sys_user_role` 关联到用户。角色决定用户可执行的权限集合。
_避免_：用户类型、身份标签

**权限**：RBAC 模型中可执行的最小操作单元（如 `report:handle`、`post:delete:any`），由 `sys_permission` 表定义，通过 `sys_role_permission` 关联到角色。
_避免_：功能开关、菜单权限

**验证码**：登录时发送到用户手机的 6 位数字凭证，存储在 Redis 中并带有 TTL，同一手机号 60 秒内不可重复发送。通过 `SmsSender` 接口抽象发送通道。
_避免_：短信（短信是发送通道，不是凭证本身）、动态码

**首次登录**：手机号第一次完成验证并建立社区身份的行为。
_避免_：注册流程（当前首次登录同时完成身份创建）

**立即发布**：用户提交合规的帖子后，帖子直接进入对访问者可见的已发布状态。
_避免_：自动审核（是否违规是管理员后续处理的事项）

**内容举报**：用户针对疑似违规的帖子或评论提交的治理请求，包含被举报内容、举报理由和提交者。
_避免_：投诉（语义过于宽泛，无法明确社区治理对象）

**审核处理**：管理员对内容举报进行查看、确认或驳回，并在确认违规时采取内容处置的行为。
_避免_：自动审核（当前由管理员作出处理决定）

**内容处置**：针对违规帖子或评论采取的隐藏、删除或恢复等可追踪治理结果。
_避免_：物理删除（是否保留数据是实现方式，不是治理结果）

**内容所有权**：作者对自己创建的帖子或评论拥有的管理范围，包括修改或删除权限。
_避免_：管理员权限（管理员是另一种社区治理权限）

**只读互动统计**：帖子当前已经产生的点赞、收藏或关注等数量展示，不代表第一阶段已经支持对应的写操作。
_避免_：互动功能（会误解为已经具备完整闭环）

**点赞关系**：用户与帖子之间表示“喜欢这篇帖子”的互动关系；同一用户对同一帖子最多保留一条关系。
_避免_：点赞数（点赞数是统计结果，不是关系本身）

**收藏关系**：用户与帖子之间表示“稍后还要查看这篇帖子”的互动关系；同一用户对同一帖子最多保留一条关系。
_避免_：收藏夹（页面或内容集合概念，不是关系本身）

**关注关系**：用户与用户之间表示“希望持续看到对方内容”的互动关系；关注者和被关注者是两个不同角色。
_避免_：粉丝（粉丝是被关注者的集合或用户称呼，不是行为关系）

**互动状态**：从当前登录会话视角描述用户是否已点赞、已收藏某帖子或已关注某用户。
_避免_：互动统计（统计是数量，状态是当前用户的布尔判断）

**已删除评论**：作者删除后不再展示原文、但仍保留在讨论位置上的评论记录。
_避免_：评论不存在（该说法会丢失讨论结构）

**正常用户**：当前允许登录并参与社区写操作的用户状态。
_避免_：活跃用户（“活跃”可能仅表示近期使用过）

## 界面语言

**社区内容流**：首页按时间或推荐顺序展示帖子的主要阅读区域，由作者信息、帖子内容和互动状态组成。
_避免_：卡片墙、信息卡片（它们描述视觉实现，不能表达业务用途）

**发布编辑器**：用户创建或修改帖子的工作区域，包含标题、正文、内容分类、内容标签、图片和发布操作。
_避免_：发布表单（只描述控件形态，不能覆盖编辑与草稿行为）

**本地草稿**：发布编辑器保存在当前浏览器中的未发布内容，不进入服务端，也不属于已发布帖子。
_避免_：草稿帖子（当前后端不存在草稿状态）

## 2026-09-16 单体部署基线决策

- 当前 MVP 先保持单个 Spring Boot 后端和独立 Next.js 前端，不提前拆分微服务。
- 下一阶段目标是 Docker Compose 本地可重复启动，服务包含 MySQL、后端和前端。
- MySQL 使用数据卷，后端上传目录使用持久化卷，Flyway 仍是唯一数据库迁移入口。
- ~~本阶段保持 Cookie 会话和现有 REST API，不引入 Redis、MQ、MinIO、Elasticsearch、Nacos 或 Gateway。~~（已被 2026-09-20 决策部分取代）
- 部署规格写入 `docs/specs/single-monolith-deployment-baseline.md`。

## 2026-09-20 微服务演进启动决策

- 项目目标从"完成社区功能"转向"以项目为载体实践后端难点，用微服务组件解决，写进求职简历"。
- 参照犬小哈《Spring Cloud Alibaba 小哈书（仿小红书）微服务项目实战》专栏。
- **引入 Redis**：用于分布式会话（SaToken）、验证码存储、权限缓存、后续计数服务和搜索引擎。
- **引入 SaToken**：完全替代 Spring Security，管理 Token、会话、路由拦截和注解鉴权。
- **引入 RBAC 权限模型**：四张表（sys_role / sys_permission / sys_user_role / sys_role_permission），替代硬编码枚举。
- **保持 Cookie 模式**：Token 通过 HttpOnly Cookie（`KUROS_SESSION`）传递，前端零改动。
- **保持单体部署**：本步骤不拆微服务、不引入 Nacos/Gateway，只在单体内替换认证基础设施。
- 架构决策记录：`docs/adr/0002-distributed-session-satoken-redis.md`。
- 演进路线：Redis+SaToken → MinIO → Nacos/Gateway → 服务拆分 → RocketMQ → ES+Canal → Sentinel → XXL-JOB → Cassandra+Leaf → Jmeter+CI/CD。
- 分工规则：中间件下载/安装/启动由用户执行，代码/配置/测试由 AI 编写，vibe learning 模式。
- 本决策**不引入**：MQ、MinIO、Elasticsearch、Nacos、Gateway、Sentinel（留到后续切片）。

## 2026-09-21 服务拆分决策（切片 #10）

- 拆出用户域微服务 `kuros-user`：认证、验证码、RBAC（四表 + StpInterface）、用户资料实体、关注关系；独立数据库 `kuros_user`（同 MySQL 实例），backend 新增迁移 DROP 迁走的表。
- **组合视图留在内容域组装**（公开资料、个人中心、用户帖子列表），经 OpenFeign 取用户数据；依赖方向固定为**内容域 → 用户域**，不制造双向依赖。
- 跨库无外键：一致性降级为服务契约（约定 ID 对齐种子 + 降级占位 + 生产事件驱动，事件驱动留 RocketMQ 切片）。
- 登录态零迁移：两个服务共享同一 Redis + 同名 Cookie（`KUROS_SESSION`）+ 相同 SaToken 配置。
- 网关路由：`/api/v1/auth/**`、`/api/v1/users/*/follow` → `lb://kuros-user`；其余 → `lb://kuros-backend`。
- 架构决策记录：`docs/adr/0003-user-service-split.md`；实现规格：`docs/slice10-service-split-spec.md`。

### 术语补充

**用户域**：认证、会话、RBAC 权限、用户资料、关注关系的归属边界，由 kuros-user 服务拥有。
_避免_：账户中心（口语化，边界不明确）

**内容域**：帖子、评论、互动、举报、媒体的归属边界，由 kuros-backend（内容服务）拥有；组合视图也在此域组装。
_避免_：主服务、核心服务（暗示等级，而非边界）
