# Issue 标签约定

| 标签 | 用途 |
| --- | --- |
| `needs-triage` | 新 Issue，等待确认范围和优先级 |
| `needs-info` | 缺少复现步骤、设计或技术信息 |
| `ready-for-agent` | 范围、验收标准和依赖已明确，可以开始实现 |
| `ready-for-human` | 需要用户做产品、账号、素材或架构决策 |
| `wontfix` | 明确不处理 |

功能域标签建议使用 `area:frontend`、`area:backend`、`area:docs`；阶段标签与功能域标签可以同时存在。
